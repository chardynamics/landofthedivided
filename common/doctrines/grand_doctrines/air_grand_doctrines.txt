air_power_projection = {
	folder = air

	name = GRAND_DOCTRINE_AIR_POWER_PROJECTION
	description = GRAND_DOCTRINE_AIR_POWER_PROJECTION_DESC
	icon = GFX_air_supremacy_medium
	available = {
		always = yes
	}

	xp_cost = 100
	xp_type = air

	ai_will_do = {
		base = 1
	}

	tracks = {
		fighter_aircraft
		# helicopter
		bomber_aircraft
		operations_aerial
	}

	# EFFECTS
	
	air_range_factor = 0.1
	air_strategic_bomber_defence_factor = 0.1
	air_fuel_consumption_factor = 0.05

	milestones = {
		{
			#1 Fighters
			category_fighter = {
				air_agility = 0.1
			}
			air_interception_detect_factor = 0.2

			effect = {
				if = {
					limit = {
						NOT = {
							has_variable = fighters_milestone_var
						}
					}
					set_variable = { fighters_milestone_var = 1 }
				}
				else = {
					add_to_variable = { fighters_milestone_var = 1 }
				}
			}
		}
		# {
		# 	#2 Helicopters
		# 	air_cas_present_factor = 0.15
		# 	army_bonus_air_superiority_factor = 0.1

		# 	effect = {
		# 		if = {
		# 			limit = {
		# 				NOT = {
		# 					has_variable = heli_milestone_var
		# 				}
		# 			}
		# 			set_variable = { heli_milestone_var = 1 }
		# 		}
		# 		else = {
		# 			add_to_variable = { heli_milestone_var = 1 }
		# 		}
		# 	}
		# }
		{
			#3 Bomber Aircraft
			category_tac_bomber = {
				air_bombing = 0.2
			}
			air_strategic_bomber_defence_factor = 0.15
			strategic_bomb_visibility = -0.4

			effect = {
				if = {
					limit = {
						NOT = {
							has_variable = bombers_milestone_var
						}
					}
					set_variable = { bombers_milestone_var = 1 }
				}
				else = {
					add_to_variable = { bombers_milestone_var = 1 }
				}
			}
		}
		{
			#4 Operations
			air_mission_xp_gain_factor = 0.15
			air_ace_generation_chance_factor = 0.10
			effect = {
				if = {
					limit = {
						NOT = {
							has_variable = operations_aerial_milestone_var
						}
					}
					set_variable = { operations_aerial_milestone_var = 1 }
				}
				else = {
					add_to_variable = { operations_aerial_milestone_var = 1 }
				}
			}
		}
	}
}

tactical_aviation = {
	folder = air

	name = GRAND_DOCTRINE_TACTICAL_AVIATION
	description = GRAND_DOCTRINE_TACTICAL_AVIATION_DESC
	icon = GFX_medium_range_missions_medium
	available = {
		always = yes
	}

	xp_cost = 100
	xp_type = air

	ai_will_do = {
		base = 1
	}

	tracks = {
		fighter_aircraft
		# helicopter
		bomber_aircraft
		operations_aerial
	}

	# EFFECTS
	
	#
	air_agility_factor = 0.05
	air_accidents_factor = -0.05

	milestones = {
		{
			#1 Fighters
			industrial_expertise_monthly_rate = 0.5
			effect = {
				if = {
					limit = {
						NOT = {
							has_variable = fighters_milestone_var
						}
					}
					set_variable = { fighters_milestone_var = 1 }
				}
				else = {
					add_to_variable = { fighters_milestone_var = 1 }
				}
			}
		}
		# {
		# 	#2 Helicopters
		# 	air_cas_present_factor = 0.15
		# 	army_bonus_air_superiority_factor = 0.1

		# 	effect = {
		# 		if = {
		# 			limit = {
		# 				NOT = {
		# 					has_variable = heli_milestone_var
		# 				}
		# 			}
		# 			set_variable = { heli_milestone_var = 1 }
		# 		}
		# 		else = {
		# 			add_to_variable = { heli_milestone_var = 1 }
		# 		}
		# 	}
		# }
		{
			#3 Bomber Aircraft
			industrial_equipment_monthly_rate = 0.5
			effect = {
				
				if = {
					limit = {
						NOT = {
							has_variable = bombers_milestone_var
						}
					}
					set_variable = { bombers_milestone_var = 1 }
				}
				else = {
					add_to_variable = { bombers_milestone_var = 1 }
				}
			}
		}
		{
			#4 Operations
			research_speed_factor = 0.05
			effect = {
				if = {
					limit = {
						NOT = {
							has_variable = operations_aerial_milestone_var
						}
					}
					set_variable = { operations_aerial_milestone_var = 1 }
				}
				else = {
					add_to_variable = { operations_aerial_milestone_var = 1 }
				}
			}
		}
	}
}