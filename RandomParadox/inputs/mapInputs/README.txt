Climate.txt is an example file to provide colour mappings to terrain types, so the generator understands the input terrain map.
terrain.png is an example image that can be used as a basic shape to generate a scenario on.
heightmap.bmp is an example input map.